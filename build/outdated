#!/usr/bin/env bash

set -eo pipefail

SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &>/dev/null && pwd)

(
  cd "${SCRIPT_DIR}"/.. || exit 1
  echo "🏮 Checking deps.edn"
  clojure -M:outdated
  cd nvd || exit 1
  echo "🏮 Checking nvd/deps.edn"
  clojure -M:outdated
)
